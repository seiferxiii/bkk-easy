import React from 'react';
import logo from '../logo2.svg';

const InfoContainer = () => {
  return (
    <div>
      <img src={logo} alt="Onchain logo" />
    </div>
  );
};

export default InfoContainer;
